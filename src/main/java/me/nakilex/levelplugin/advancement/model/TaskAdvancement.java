package me.nakilex.levelplugin.advancement.model;

public class TaskAdvancement extends BaseAdvancement {
    public TaskAdvancement(AdvancementKey key, AdvancementDisplay display, int maxProgress, Advancement parent) {
        super(key, display, maxProgress, parent);
    }
}
