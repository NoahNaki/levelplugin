package me.nakilex.levelplugin.cooking.model;

/** Supported cooking stage definitions mapped to runtime executors when implemented. */
public enum CookingStageType {
    INSERT_ITEM,
    WAIT,
    MINI_GAME
}
