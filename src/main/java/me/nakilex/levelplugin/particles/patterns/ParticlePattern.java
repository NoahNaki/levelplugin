package me.nakilex.levelplugin.particles.patterns;

import me.nakilex.levelplugin.particles.ParticleRenderContext;

public interface ParticlePattern {
    void render(ParticleRenderContext context);
}
