package me.nakilex.levelplugin.particles.presets;

import me.nakilex.levelplugin.particles.ParticlePresetRegistry;

public interface ParticlePresetProvider {
    void register(ParticlePresetRegistry registry);
}
